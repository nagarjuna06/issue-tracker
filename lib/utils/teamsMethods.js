const teamsData = async (email) => {
  return teams;
};

const joinTeamsData = async (email) => {
  return teams;
};

const getTeamsData = async (teamType, email) => {
  return data;
};

export default getTeamsData;
